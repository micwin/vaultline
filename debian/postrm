#!/usr/bin/env bash
set -euo pipefail

STATE_FILE="/var/lib/vaultline/instances.state"

if [[ "$1" = "purge" ]]; then
  rm -f "${STATE_FILE}"
  if [[ -n "${SUDO_USER:-}" ]]; then
    rm -rf "/home/${SUDO_USER}/.local/share/vaultline" "/home/${SUDO_USER}/.config/vaultline"
  fi
fi
