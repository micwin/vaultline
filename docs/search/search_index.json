{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"vaultline docs","text":"<p>This documentation complements the main release site and focuses on hands-on operation.</p>"},{"location":"#table-of-contents","title":"Table of contents","text":"<ul> <li>Start here</li> <li>Typical flow</li> <li>Operational checklist</li> <li>Related links</li> </ul>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Use CLI for command usage and completion behavior.</li> <li>Use Daemon &amp; API for process behavior, service integration, and endpoint details.</li> <li>Use Store model for multi-store semantics and storage layout.</li> <li>Use Backup &amp; restore for archive and recovery operations.</li> <li>Use Runbook (Project Workflow) for release/versioning workflow and helper scripts.</li> </ul>"},{"location":"#typical-flow","title":"Typical flow","text":"<ol> <li>Start and verify daemon readiness (<code>vaultline daemon</code>, <code>vaultline health</code>) so API and store state are visible before any secret operation.    See: Daemon &amp; API.</li> <li>Initialize/register stores (<code>store init</code>, <code>store add</code>) to define store boundaries and passphrase handling early.    See: Store model and CLI.</li> <li>Unseal only required stores (<code>store unseal</code>, optionally <code>--from-secret</code>) to minimize exposure and keep per-store access explicit.    See: Daemon &amp; API.</li> <li>Execute secret lifecycle commands (<code>secret set/get/list/glob/copy/move/delete</code>) as the primary operational interface.    See: CLI.</li> <li>Run backup/export and reseal workflow before and after sensitive changes.    See: Backup &amp; restore and CLI.</li> </ol>"},{"location":"#operational-checklist","title":"Operational checklist","text":"<ul> <li>Confirm daemon health and store status with <code>vaultline health</code>.   See: Daemon &amp; API.</li> <li>Unseal only the stores required for the current task.   See: Daemon &amp; API.</li> <li>Snapshot/backup before broad updates.   See: Backup &amp; restore.</li> <li>Perform secret changes via CLI commands.   See: CLI.</li> <li>Reseal stores according to your operating policy.   See: Store model and Daemon &amp; API.</li> </ul>"},{"location":"#related-links","title":"Related links","text":"<ul> <li>Main site overview: <code>/vaultline/</code></li> <li>Downloads and release notes: <code>/vaultline/downloads/</code></li> <li>Repository: https://github.com/micwin/vaultline</li> </ul>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#storage-layout","title":"Storage layout","text":"<ul> <li>Default store: <code>~/.local/share/vaultline/stores/default</code> (configurable via <code>--store-dir</code>)</li> <li>Named-store registry: <code>~/.config/vaultline/stores.json</code> (configurable via <code>--config-file</code>)</li> <li>By default, daemon and CLI share the same user-scoped config/data area (<code>~/.config/vaultline</code>, <code>~/.local/share/vaultline</code>) unless you override flags/paths.</li> <li>Each store has its own root:</li> <li><code>.master_salt</code> (base64 encoded)</li> <li><code>secrets/&lt;name&gt;.vlx</code></li> <li>Secret names remain lowercase and may include <code>.</code> or <code>-</code></li> <li>Fully qualified keys use the form <code>store:key</code>; only the prefix selects the store, the secret filename stays <code>key.vlx</code></li> </ul>"},{"location":"architecture/#multi-store-model","title":"Multi-store model","text":"<ul> <li><code>default</code> is always registered and points at the daemon's primary store directory.</li> <li>Additional stores are registered in <code>stores.json</code> with their own paths and (optionally) remembered passphrases.</li> <li>Every store is an independent <code>storage.Store</code>, meaning:</li> <li>separate salt</li> <li>separate passphrase</li> <li>separate seal state</li> <li>separate files on disk</li> <li>Broken or missing external stores do not block daemon startup as long as <code>default</code> is healthy. They are surfaced as <code>available=false</code> in health/status output.</li> </ul>"},{"location":"architecture/#key-derivation","title":"Key derivation","text":"<p>For each store independently: 1. User supplies passphrase via CLI, <code>VAULTLINE_PASSPHRASE</code>, seal file, or a remembered key in <code>stores.json</code> 2. Argon2id derives a 32-byte master key using that store's <code>.master_salt</code> 3. Each secret name feeds <code>HMAC-SHA256(master, name)</code>; the result seeds XChaCha20-Poly1305 4. Sealing zeroes the in-memory master key 5. Unless <code>--keep-keys</code> is used, sealing also removes any remembered passphrase from the registry config</p>"},{"location":"architecture/#api-surface","title":"API surface","text":"<p>Legacy default-store routes (kept so older/default-store-only workflows continue to work) target <code>default</code>: - <code>GET /api/v1/health</code> - <code>POST /api/v1/unseal</code> - <code>POST /api/v1/seal</code> - <code>PUT/GET/DELETE /api/v1/secrets/{name}</code></p> <p>Named-store routes: - <code>GET /api/v1/stores</code> - <code>POST /api/v1/stores</code> - <code>GET /api/v1/stores/{store}</code> - <code>POST /api/v1/stores/{store}/unseal</code> - <code>POST /api/v1/stores/{store}/seal</code> - <code>PUT/GET/DELETE /api/v1/stores/{store}/secrets/{name}</code> - <code>GET /api/v1/stores/{store}/secrets</code></p>"},{"location":"architecture/#remembered-passphrases","title":"Remembered passphrases","text":"<ul> <li><code>store init</code> generates a random passphrase, stores it in the registry config, and leaves the new store unsealed.</li> <li><code>store unseal &lt;name&gt;</code> first tries the remembered passphrase before prompting.</li> <li><code>seal</code> removes remembered passphrases unless <code>--keep-keys</code> is requested.</li> </ul>"},{"location":"architecture/#dashboard-and-health","title":"Dashboard and health","text":"<p><code>GET /</code> renders a static HTML page with version plus one line per configured store (<code>sealed</code>, <code>unsealed</code>, or <code>unavailable</code>).</p> <p><code>GET /api/v1/health</code> returns the same status in JSON and is intended to stay readable even when one project-specific store is missing.</p>"},{"location":"architecture/#testing","title":"Testing","text":"<p>Smokey suites live under <code>tests.d/</code> and spin up the daemon in <code>.testrun/</code>. They now cover both the default <code>local</code> store and an additional named store, including <code>store init</code>, <code>store unseal</code>, and prefixed <code>secret get/set/list</code> flows.</p>"},{"location":"backup-restore/","title":"Backup &amp; restore","text":"<p>Operational guidance for creating and restoring full-store archives.</p>"},{"location":"backup-restore/#table-of-contents","title":"Table of contents","text":"<ul> <li>Overview</li> <li>Create backups</li> <li>Restore backups</li> <li>Recommended workflow</li> <li>Failure handling</li> </ul>"},{"location":"backup-restore/#overview","title":"Overview","text":"<p>Vaultline provides two equivalent archive workflows:</p> <ul> <li><code>backup zip &lt;store&gt; [zip-file]</code> / <code>restore zip &lt;zip-file&gt; &lt;store&gt; [--overwrite]</code></li> <li><code>export zip &lt;store&gt; [zip-file]</code> / <code>import zip &lt;zip-file&gt; &lt;store&gt;</code></li> </ul> <p><code>backup/restore</code> are the preferred operator-facing names for runbooks.</p>"},{"location":"backup-restore/#create-backups","title":"Create backups","text":"<p>Create archive with explicit filename:</p> <pre><code>vaultline backup zip mystore ./mystore-backup.zip\n</code></pre> <p>Create archive with auto filename (<code>&lt;store&gt;-YYYY-MM-DD.zip</code>):</p> <pre><code>vaultline backup zip mystore\n</code></pre>"},{"location":"backup-restore/#restore-backups","title":"Restore backups","text":"<p>Restore into store name:</p> <pre><code>vaultline restore zip ./mystore-backup.zip mystore\n</code></pre> <p>Overwrite existing target store explicitly:</p> <pre><code>vaultline restore zip ./mystore-backup.zip mystore --overwrite\n</code></pre>"},{"location":"backup-restore/#recommended-workflow","title":"Recommended workflow","text":"<ol> <li>Seal/non-critical write pause for target store if possible.</li> <li>Create backup before broad writes/imports.</li> <li>Verify backup file integrity and storage location.</li> <li>Test restore procedure periodically on a non-production store.</li> <li>Keep at least one known-good archive outside the primary host.</li> </ol>"},{"location":"backup-restore/#failure-handling","title":"Failure handling","text":"<ul> <li>If restore fails midway, inspect daemon logs and store status first.</li> <li>Re-run restore only with explicit <code>--overwrite</code> when replacement is intended.</li> <li>Keep previous backup archive until post-restore verification is complete.</li> </ul>"},{"location":"bitwarden/","title":"Bitwarden import","text":"<p>Vaultline imports Bitwarden data through the official <code>bw</code> CLI.</p>"},{"location":"bitwarden/#requirements","title":"Requirements","text":"<ul> <li><code>bw</code> is installed and available on <code>PATH</code></li> <li><code>BW_SESSION</code> is set for an unlocked Bitwarden session</li> </ul>"},{"location":"bitwarden/#commands","title":"Commands","text":"<pre><code>vaultline import bitwarden --all --store default\nvaultline import bitwarden --item \"GitHub\" --store default\n</code></pre> <p>Useful flags:</p> <ul> <li><code>--prefix &lt;prefix&gt;</code></li> <li><code>--dry-run</code></li> <li><code>--add-missing-keys</code></li> <li><code>--overwrite-existing-keys</code></li> </ul>"},{"location":"bitwarden/#mapping-notes","title":"Mapping notes","text":"<ul> <li>Imports include usernames, passwords, notes, URIs, TOTP, and custom fields.</li> <li>Key normalization is intentionally conservative.</li> <li>Invalid generated keys are reported and skipped so source data can be corrected explicitly.</li> </ul>"},{"location":"bitwarden/#recommended-workflow","title":"Recommended workflow","text":"<ol> <li>Run with <code>--dry-run</code>.</li> <li>Review skipped/invalid key reports.</li> <li>Import without <code>--dry-run</code> once key mapping looks correct.</li> </ol>"},{"location":"cli/","title":"CLI","text":"<p>Most day-to-day vaultline operations are initiated through the CLI.</p>"},{"location":"cli/#table-of-contents","title":"Table of contents","text":"<ul> <li>Global flags</li> <li>Top-level commands</li> <li>Secret commands</li> <li>Store commands</li> <li>Daemon listener commands</li> <li>Completion</li> <li>Diagnostics</li> <li>Related pages</li> </ul>"},{"location":"cli/#global-flags","title":"Global flags","text":"<ul> <li><code>--addr 127.0.0.1:8428</code> \u2014 daemon address (default loopback)</li> <li><code>--output json|text|raw</code> \u2014 output format for supported commands</li> </ul>"},{"location":"cli/#top-level-commands","title":"Top-level commands","text":"<ul> <li><code>version</code> \u2014 prints CLI version only.</li> <li><code>health</code> \u2014 prints daemon and store status.</li> <li><code>seal</code> \u2014 seals the default store.</li> <li><code>unseal</code> \u2014 unseals the default store.</li> <li><code>daemon-stop</code> \u2014 asks daemon process to stop.</li> <li><code>daemon ...</code> \u2014 manages extra listeners and allow rules.</li> <li><code>store ...</code> \u2014 manages named stores and their seal lifecycle.</li> <li><code>secret ...</code> \u2014 manages secret CRUD/search/transfer operations.</li> <li><code>import ...</code> \u2014 imports data from external sources (<code>bitwarden</code>, <code>zip</code>).</li> <li><code>export ...</code> \u2014 exports store data (<code>zip</code>).</li> <li><code>backup ...</code> \u2014 creates full-store backups.</li> <li><code>restore ...</code> \u2014 restores full-store backups.</li> <li><code>completion ...</code> \u2014 prints shell completion scripts.</li> </ul> <p>Detailed backup/restore workflow is documented on Backup &amp; restore.</p>"},{"location":"cli/#secret-commands","title":"Secret commands","text":"<ul> <li><code>secret set &lt;store:key&gt; [--value|--file|--stdin] [--twice]</code> \u2014 create/update one secret.</li> <li><code>secret get &lt;store:key&gt; [--out path] [--output raw|json]</code> \u2014 read one secret.</li> <li><code>secret delete &lt;store:key&gt;</code> \u2014 delete one secret.</li> <li><code>secret list [store:] [--output json] [--raw]</code> \u2014 list store keys.</li> <li><code>secret glob &lt;store-glob:key-glob&gt; [--raw]</code> \u2014 search keys by wildcard.</li> <li><code>secret delete-prefix &lt;store:prefix.&gt; [--dry-run] [--yes]</code> \u2014 delete key ranges by prefix.</li> <li><code>secret copy [oldstore:]oldname [newstore:]newname [--force]</code> \u2014 copy secret between keys/stores.</li> <li><code>secret move [oldstore:]oldname [newstore:]newname [--force]</code> \u2014 move secret between keys/stores.</li> </ul>"},{"location":"cli/#store-commands","title":"Store commands","text":"<ul> <li><code>store add &lt;name&gt; &lt;path&gt;</code> \u2014 register an existing store path.</li> <li><code>store init &lt;name&gt; [path] [--prompt-passphrase] [--remember-passphrase]</code> \u2014 create/register a new store.</li> <li><code>store list [--raw]</code> \u2014 list configured stores and status.</li> <li><code>store show &lt;name&gt;</code> \u2014 show one store record.</li> <li><code>store unseal &lt;name&gt; [--prompt-passphrase] [--remember-passphrase|--transient] [--from-secret store:key]</code> \u2014 unseal named store.</li> <li><code>store seal &lt;name&gt; [--keep-keys]</code> \u2014 seal named store.</li> <li><code>store delete|remove|rm &lt;name&gt;</code> \u2014 remove store registration.</li> </ul>"},{"location":"cli/#daemon-listener-commands","title":"Daemon listener commands","text":"<ul> <li><code>daemon bind &lt;addr&gt;</code> \u2014 add extra listener.</li> <li><code>daemon list-binds</code> \u2014 list extra listeners.</li> <li><code>daemon unbind &lt;addr&gt;</code> \u2014 remove listener and its allow rules.</li> <li><code>daemon allow &lt;addr&gt; &lt;cidr-or-ip&gt;</code> \u2014 allow one source for one listener.</li> <li><code>daemon list-allows &lt;addr&gt;</code> \u2014 list listener allow rules.</li> <li><code>daemon unallow &lt;addr&gt; &lt;cidr-or-ip&gt;</code> \u2014 remove one allow rule.</li> </ul>"},{"location":"cli/#completion","title":"Completion","text":"<ul> <li><code>vaultline completion bash</code> \u2014 emits bash completion.</li> <li><code>vaultline completion zsh</code> \u2014 emits zsh completion.</li> </ul> <p>Completion supports both <code>vaultline</code> and <code>vl</code>, including dynamic suggestions for stores, listener addresses, allow rules, and qualified keys.</p>"},{"location":"cli/#diagnostics","title":"Diagnostics","text":"<ul> <li><code>vaultline health</code></li> <li><code>curl http://127.0.0.1:8428/</code></li> </ul> <p>CLI exits non-zero when daemon is unavailable, target store is sealed, or operations fail.</p>"},{"location":"cli/#related-pages","title":"Related pages","text":"<ul> <li>Daemon &amp; API</li> <li>Store model</li> <li>Backup &amp; restore</li> <li>Bitwarden import</li> <li>Runbook (Project Workflow)</li> </ul>"},{"location":"daemon/","title":"Daemon &amp; API","text":"<p>The daemon is the stateful process behind all CLI operations and the HTTP API surface.</p>"},{"location":"daemon/#table-of-contents","title":"Table of contents","text":"<ul> <li>Start and process model</li> <li>Systemd/deb integration</li> <li>Configuration and paths</li> <li>Unseal behavior</li> <li>HTTP API</li> <li>Error model</li> </ul>"},{"location":"daemon/#start-and-process-model","title":"Start and process model","text":"<pre><code>vaultline daemon --store-dir ./store\n</code></pre> <p>Default bind address is <code>127.0.0.1:8428</code>.</p> <p>Key flags:</p> <ul> <li><code>--addr HOST:PORT</code> \u2014 daemon listener (default loopback)</li> <li><code>--store-dir DIR</code> \u2014 default store path</li> <li><code>--config-file PATH</code> \u2014 named-store registry (<code>stores.json</code>)</li> <li><code>--daemon-config-file PATH</code> \u2014 daemon bind/allow config (<code>daemon.json</code>)</li> <li><code>--seal-file PATH</code> \u2014 startup passphrase source for default store</li> </ul>"},{"location":"daemon/#systemddeb-integration","title":"Systemd/deb integration","text":"<p>Systemd unit template in source:</p> <ul> <li><code>systemd/vaultline@.service</code></li> </ul> <p>The Debian package manages service instances during upgrade:</p> <ul> <li><code>prerm</code> records current <code>vaultline@*.service</code> state, then stops/disables units.</li> <li><code>postinst</code> restores enable/start state and reloads systemd.</li> </ul> <p>Because daemon processes are restarted during package upgrades, stores that are not automatically unsealed may come back sealed after update.</p>"},{"location":"daemon/#configuration-and-paths","title":"Configuration and paths","text":"<p>From the packaged systemd service template, runtime paths are per-user under home directories:</p> <ul> <li>Default store path: <code>/home/&lt;user&gt;/.local/share/vaultline/stores/default</code></li> <li>Seal file: <code>/home/&lt;user&gt;/.config/vaultline/seal</code></li> <li>Store registry: <code>/home/&lt;user&gt;/.config/vaultline/stores.json</code></li> <li>Daemon bind/allow config: <code>/home/&lt;user&gt;/.config/vaultline/daemon.json</code></li> </ul> <p>So config is not in <code>/etc/vaultline</code> with current packaging.</p>"},{"location":"daemon/#unseal-behavior","title":"Unseal behavior","text":"<ul> <li><code>VAULTLINE_PASSPHRASE</code> applies to the default store only.</li> <li>Named stores are unsealed via <code>vaultline store unseal &lt;name&gt; ...</code>.</li> <li><code>store unseal --from-secret &lt;store:key&gt;</code> reads unseal material from another store.</li> <li>If no explicit passphrase is supplied, daemon unseal tries remembered passphrase material first.</li> </ul>"},{"location":"daemon/#http-api","title":"HTTP API","text":""},{"location":"daemon/#get-apiv1health","title":"<code>GET /api/v1/health</code>","text":"<p>Returns daemon and store status summary.</p>"},{"location":"daemon/#get-apiv1stores","title":"<code>GET /api/v1/stores</code>","text":"<p>Lists configured stores with path, availability, seal state, and remembered-key state.</p>"},{"location":"daemon/#post-apiv1stores","title":"<code>POST /api/v1/stores</code>","text":"<p>Create/register a named store (<code>initialize=true</code> creates and unseals immediately).</p>"},{"location":"daemon/#get-apiv1storesstore","title":"<code>GET /api/v1/stores/{store}</code>","text":"<p>Returns one store entry.</p>"},{"location":"daemon/#post-apiv1storesstoreunseal","title":"<code>POST /api/v1/stores/{store}/unseal</code>","text":"<p>Request body:</p> <pre><code>{ \"passphrase\": \"...\", \"remember_passphrase\": false }\n</code></pre>"},{"location":"daemon/#post-apiv1storesstoreseal","title":"<code>POST /api/v1/stores/{store}/seal</code>","text":"<p>Request body:</p> <pre><code>{ \"keep_keys\": false }\n</code></pre>"},{"location":"daemon/#get-apiv1storesstoresecrets","title":"<code>GET /api/v1/stores/{store}/secrets</code>","text":"<p>List all keys in one store.</p>"},{"location":"daemon/#put-apiv1storesstoresecretsname","title":"<code>PUT /api/v1/stores/{store}/secrets/{name}</code>","text":"<p>Store/update one secret (<code>{ \"value\": \"base64\" }</code>).</p>"},{"location":"daemon/#get-apiv1storesstoresecretsname","title":"<code>GET /api/v1/stores/{store}/secrets/{name}</code>","text":"<p>Fetch one secret (<code>{ \"value\": \"base64\", \"version\": \"...\" }</code>).</p>"},{"location":"daemon/#delete-apiv1storesstoresecretsname","title":"<code>DELETE /api/v1/stores/{store}/secrets/{name}</code>","text":"<p>Delete one secret.</p>"},{"location":"daemon/#error-model","title":"Error model","text":"<p>Errors follow:</p> <pre><code>{ \"error\": \"CODE\", \"message\": \"description\" }\n</code></pre> <p>Common codes include:</p> <ul> <li><code>SEALED</code></li> <li><code>STORE_NOT_FOUND</code></li> <li><code>STORE_UNAVAILABLE</code></li> <li><code>INVALID_IDENTIFIER</code></li> <li><code>NOT_FOUND</code></li> <li><code>STORE_ERROR</code></li> </ul>"},{"location":"developer/","title":"Developer ramp-up","text":"<p>Developer onboarding for local work on vaultline.</p>"},{"location":"developer/#table-of-contents","title":"Table of contents","text":"<ul> <li>Prerequisites</li> <li>Initial setup</li> <li>Test workflow</li> <li>Site/docs workflow</li> <li>Release handoff expectations</li> </ul>"},{"location":"developer/#prerequisites","title":"Prerequisites","text":"<ul> <li>Go 1.22+</li> <li>Python + MkDocs Material (for docs build)</li> <li>Ruby + Bundler (for Jekyll pages build)</li> </ul>"},{"location":"developer/#initial-setup","title":"Initial setup","text":"<pre><code>./scripts/bootstrap-debian.sh\n</code></pre> <p>This installs core tooling and Smokey.</p>"},{"location":"developer/#test-workflow","title":"Test workflow","text":"<pre><code>go test ./...\nsmokey --tests-dir tests.d\n</code></pre> <p>Use focused package tests first, then full suite before release prep.</p>"},{"location":"developer/#sitedocs-workflow","title":"Site/docs workflow","text":"<pre><code>./scripts/build-site.sh\n./scripts/serve-site.sh\n</code></pre> <p><code>build-site.sh</code> produces <code>site/</code> with Jekyll + MkDocs output.</p>"},{"location":"developer/#release-handoff-expectations","title":"Release handoff expectations","text":"<ul> <li>Work on <code>develop</code>.</li> <li>Keep release-note snippets under <code>site-src/release-notes/unreleased/</code>.</li> <li>Hand off changes via PR/commit before running release scripts.</li> <li>Use Runbook (Project Workflow) for exact release sequence.</li> </ul>"},{"location":"runbook/","title":"Runbook (Project Workflow)","text":"<p>This runbook describes repository workflow and release operations.</p>"},{"location":"runbook/#table-of-contents","title":"Table of contents","text":"<ul> <li>Development cycle</li> <li>Versioning</li> <li>Release flow</li> <li>Site/docs workflow</li> <li>Helper scripts</li> <li>Operations references</li> <li>Operational hardening checklist</li> <li>Developer references</li> </ul>"},{"location":"runbook/#development-cycle","title":"Development cycle","text":"<ol> <li>Work on <code>develop</code>.</li> <li>Keep commits small and focused.</li> <li>Run tests/build locally before release prep.</li> </ol>"},{"location":"runbook/#versioning","title":"Versioning","text":"<ul> <li>Version is stored in <code>pkg/version/version.go</code>.</li> <li>Release prep updates this version and produces artifacts.</li> <li>Version bump is automatic as part of <code>./scripts/prepare-release.sh &lt;X.Y.Z&gt;</code>.</li> <li>Do release prep on <code>develop</code>, not directly on <code>release</code>, to avoid branch/history confusion and accidental out-of-sequence version updates.</li> </ul>"},{"location":"runbook/#release-flow","title":"Release flow","text":"<ol> <li>Prepare release content and artifacts:</li> </ol> <p><code>bash    ./scripts/prepare-release.sh &lt;X.Y.Z&gt;</code></p> <ol> <li>Review and commit generated changes on <code>develop</code>.</li> <li>Push <code>develop</code>.</li> <li>Fast-forward/push <code>release</code>:</li> </ol> <p><code>bash    ./scripts/release.sh</code></p> <ol> <li>GitHub Actions builds <code>.deb</code> + binary, tags <code>vX.Y.Z</code>, publishes release, and deploys Pages.</li> </ol> <p>Practical guardrail:</p> <ul> <li>Avoid manual version edits on <code>release</code> branch; always flow through <code>develop -&gt; release</code>.</li> </ul>"},{"location":"runbook/#sitedocs-workflow","title":"Site/docs workflow","text":"<ul> <li>Main site content: <code>site-src/</code> (Jekyll)</li> <li>Docs content: <code>docs/</code> (MkDocs Material)</li> </ul> <p>Local build:</p> <pre><code>./scripts/build-site.sh\n</code></pre> <p>Local serve:</p> <pre><code>./scripts/serve-site.sh\n</code></pre>"},{"location":"runbook/#helper-scripts","title":"Helper scripts","text":"<ul> <li><code>scripts/build.sh</code> \u2014 central orchestrator for compile/package/site tasks.</li> <li><code>scripts/build-site.sh</code> \u2014 builds static site output (<code>site/</code>) from Jekyll (<code>site-src/</code>) and MkDocs (<code>docs/</code>).</li> <li><code>scripts/serve-site.sh</code> \u2014 convenience wrapper to build then serve locally.</li> <li><code>scripts/ghpages-serve.sh</code> \u2014 static HTTP server for already-built output, including <code>/vaultline/docs/</code>.</li> <li><code>scripts/prepare-release.sh</code> \u2014 bumps to target version, builds artifacts, merges unreleased notes into versioned notes, updates downloads metadata.</li> <li><code>scripts/release.sh</code> \u2014 fast-forwards <code>release</code> from <code>develop</code> and pushes it to trigger CI release/publish.</li> <li><code>scripts/publish-release.sh</code> \u2014 push helper when already on <code>release</code>.</li> </ul>"},{"location":"runbook/#operations-references","title":"Operations references","text":"<ul> <li>Backup/restore procedure: Backup &amp; restore</li> </ul>"},{"location":"runbook/#operational-hardening-checklist","title":"Operational hardening checklist","text":"<ul> <li>Keep daemon listeners on loopback by default; only add remote binds deliberately.</li> <li>Use allowlists for every non-loopback listener (<code>daemon allow ...</code>), and review them regularly.</li> <li>Keep stores sealed by default and unseal only what is needed for the current task.</li> <li>Prefer cross-store unseal material (<code>store unseal --from-secret</code>) over manual passphrase sharing.</li> <li>Backup before broad writes/imports and verify restore workflow periodically.   See: Backup &amp; restore.</li> <li>Rotate sensitive passphrases/secrets on a regular cadence and after operator or host changes.</li> <li>Restrict filesystem access to store/config paths (<code>~/.local/share/vaultline</code>, <code>~/.config/vaultline</code>).</li> <li>After package upgrades/restarts, verify health and seal state explicitly (<code>vaultline health</code>).</li> </ul>"},{"location":"runbook/#developer-references","title":"Developer references","text":"<ul> <li>Developer ramp-up</li> </ul>"},{"location":"store-model/","title":"Store model","text":"<p>Vaultline supports one default store plus any number of named stores.</p>"},{"location":"store-model/#key-addressing","title":"Key addressing","text":"<ul> <li>Default store key: <code>infra.db-password</code></li> <li>Named store key: <code>project-a:infra.db-password</code></li> </ul> <p>The <code>store:</code> prefix selects the store. The key segment remains the on-disk secret name.</p>"},{"location":"store-model/#store-isolation","title":"Store isolation","text":"<p>Each store has independent:</p> <ul> <li><code>.master_salt</code></li> <li>passphrase</li> <li>seal state</li> <li>secret files</li> </ul> <p>This means one store can be sealed/unsealed without affecting others.</p>"},{"location":"store-model/#remembered-passphrases","title":"Remembered passphrases","text":"<ul> <li><code>store init</code> can persist generated passphrases in the local registry.</li> <li><code>store seal</code> removes remembered keys unless <code>--keep-keys</code> is used.</li> <li><code>store unseal</code> first tries remembered material, then explicit input/prompt.</li> </ul>"},{"location":"store-model/#storage-paths","title":"Storage paths","text":"<p>See Architecture for path-level details (<code>stores.json</code>, <code>secrets/*.vlx</code>, salts).</p>"}]}