{
  "version": "0.3.41",
  "changes": [
    "GitHub release publishing now uses the versioned release-notes content as release body text.",
    "Release workflow strips Jekyll frontmatter and uploads a clean markdown body via `body_path`."
  ]
}
