---
layout: default
title: Release Notes v0.3.42
---

# Release Notes v0.3.42

## Changes

- Add `secret get --output eval-export|eval-set` so secrets can be emitted as shell assignments using a positional variable name.
- Document and test eval output in the CLI quickstart, CLI reference, and smokey suite.
